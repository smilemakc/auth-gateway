# Auth Gateway - Спецификация UI для управления

## Описание проекта

**Auth Gateway** - централизованная система аутентификации и авторизации на Go с PostgreSQL и Redis.

Система предоставляет:
- Регистрация/авторизация пользователей (email/phone + пароль)
- JWT токены (access + refresh)
- OAuth 2.0 (Google, Yandex, GitHub, Instagram, Telegram)
- Двухфакторная аутентификация (2FA TOTP)
- API ключи для сервисов
- Email/Phone верификация через OTP
- Восстановление пароля
- Аудит логирование

---

## Сущности для управления

### 1. **Пользователи (Users)**

**Поля:**
- ID (UUID)
- Email (уникальный)
- Телефон (опционально, уникальный)
- Имя пользователя (username, уникальный)
- Полное имя
- Фото профиля (URL)
- Роль: `user`, `moderator`, `admin`
- Email верифицирован (да/нет)
- Телефон верифицирован (да/нет)
- Статус активности (активен/заблокирован)
- 2FA включен (да/нет)
- Дата создания
- Дата последнего обновления

**Операции:**
- Просмотр списка (с пагинацией, фильтрами)
- Поиск по email/username/телефону
- Просмотр деталей пользователя
- Изменение роли
- Блокировка/разблокировка (is_active)
- Удаление пользователя (мягкое удаление)
- Просмотр связанных API ключей
- Просмотр OAuth аккаунтов

### 2. **API Ключи (API Keys)**

**Поля:**
- ID (UUID)
- Владелец (User ID + username)
- Название ключа
- Описание
- Префикс ключа (первые 8-12 символов для идентификации)
- Права доступа (scopes): `users:read`, `users:write`, `profile:read`, `profile:write`, `token:validate`, `token:introspect`, `admin:all`, `all`
- Дата истечения (опционально, NULL = никогда)
- Последнее использование
- Статус (активен/отозван)
- Дата отзыва (если отозван)
- Дата создания

**Операции:**
- Просмотр всех API ключей (с фильтрами по пользователю, статусу)
- Поиск по имени/префиксу
- Просмотр деталей ключа
- Отзыв ключа
- Удаление ключа
- Фильтр по истекшим/активным

### 3. **OAuth Аккаунты (OAuth Accounts)**

**Поля:**
- ID (UUID)
- Пользователь (User ID)
- Провайдер: Google, Yandex, GitHub, Instagram, Telegram
- ID в провайдере
- Дата истечения токена
- Данные профиля (JSON)
- Дата создания
- Дата обновления

**Операции:**
- Просмотр всех OAuth подключений
- Фильтр по провайдеру
- Группировка по пользователям
- Отключение OAuth аккаунта
- Просмотр данных профиля

### 4. **Журнал аудита (Audit Logs)**

**Поля:**
- ID (UUID)
- Пользователь (опционально, может быть анонимным)
- Действие: `signup`, `signin`, `signin_failed`, `signout`, `refresh_token`, `change_password`, `forgot_password`, `reset_password`, `oauth_begin`, `oauth_callback`, `update_profile`
- Тип ресурса
- ID ресурса
- IP адрес
- User-Agent
- Статус: `success`, `failed`, `blocked`
- Детали (JSON)
- Дата создания

**Операции:**
- Просмотр логов (с пагинацией)
- Фильтры: по пользователю, действию, статусу, дате
- Поиск по IP
- Экспорт логов (CSV/JSON)
- Визуализация активности (графики по времени)

### 5. **OTP коды (One-Time Passwords)**

**Поля:**
- ID (UUID)
- Email
- Тип: `verification`, `password_reset`, `2fa`, `login`
- Использован (да/нет)
- Дата истечения
- Дата создания

**Операции:**
- Просмотр активных OTP кодов
- Фильтр по типу, email
- Инвалидация OTP

### 6. **Backup коды для 2FA**

**Поля:**
- ID (UUID)
- Пользователь (User ID)
- Использован (да/нет)
- Дата использования
- Дата создания

**Операции:**
- Просмотр backup кодов пользователя
- Генерация новых кодов
- Статистика использования

---

## Статистика и дашборд

### Основные метрики:
- Общее количество пользователей
- Активные пользователи
- Пользователи с верифицированным email/phone
- Пользователи с включенным 2FA
- Общее количество API ключей (активные/отозванные)
- Количество OAuth аккаунтов
- Распределение пользователей по ролям
- Регистрации за последние 24 часа
- Входы за последние 24 часа

### Графики:
- Регистрации по дням/неделям/месяцам
- Активность входов
- Провалившиеся попытки входа
- Использование OAuth провайдеров
- Активность по типам действий (audit logs)

---

## Настройки системы

### Конфигурация, которую можно управлять через UI:

1. **OAuth провайдеры**
   - Включение/выключение провайдеров
   - Настройка Client ID и Secret
   - Redirect URLs

2. **Безопасность**
   - JWT Access Token TTL (по умолчанию 15 минут)
   - JWT Refresh Token TTL (по умолчанию 7 дней)
   - Минимальная длина пароля
   - Политика паролей (сложность)
   - Rate limiting настройки

3. **Email настройки**
   - SMTP конфигурация
   - Шаблоны писем (верификация, восстановление пароля)
   - Отправитель (from address)

4. **2FA настройки**
   - Обязательная 2FA для ролей (admin, moderator)
   - Количество backup кодов
   - Время жизни OTP кодов

5. **CORS**
   - Разрешенные origins
   - Разрешенные методы
   - Разрешенные заголовки

---

## API Endpoints для UI

Все endpoints доступны с префиксом `/auth` (HTTP) и через gRPC (порт 50051).

### Админ API (требуется роль `admin`):

```
GET    /admin/stats              - Получить статистику системы
GET    /admin/users              - Список пользователей (page, page_size)
GET    /admin/users/:id          - Детали пользователя
PUT    /admin/users/:id          - Обновить пользователя (role, is_active)
DELETE /admin/users/:id          - Удалить пользователя

GET    /admin/api-keys           - Список всех API ключей
POST   /admin/api-keys/:id/revoke - Отозвать API ключ

GET    /admin/audit-logs         - Журнал аудита (page, page_size, user_id)
```

### Пользовательские API (требуется JWT токен):

```
POST   /api-keys                 - Создать API ключ
GET    /api-keys                 - Список своих API ключей
GET    /api-keys/:id             - Детали ключа
PUT    /api-keys/:id             - Обновить ключ
POST   /api-keys/:id/revoke      - Отозвать ключ
DELETE /api-keys/:id             - Удалить ключ
```

---

## Технические детали

**Backend:**
- Go 1.23+
- Gin web framework
- gRPC для межсервисного взаимодействия
- PostgreSQL 14+ (база данных)
- Redis 7+ (кеш, rate limiting)

**Порты:**
- HTTP: 3000
- gRPC: 50051

**Аутентификация UI:**
- JWT токены через `/auth/signin`
- Refresh токен для продления сессии

**Rate Limits:**
- Регистрация: 5 запросов/час с IP
- Вход: 10 запросов/15 минут с IP
- API: 100 запросов/минуту

---

## Рекомендации для UI разработки

1. **Фреймворки:** React, Vue, или Angular
2. **UI библиотеки:** Material-UI, Ant Design, Chakra UI
3. **Таблицы:** React Table, AG Grid (для больших данных)
4. **Графики:** Chart.js, Recharts, ApexCharts
5. **Формы:** React Hook Form, Formik
6. **Состояние:** Redux Toolkit, Zustand, React Query
7. **Routing:** React Router, Vue Router

### Ключевые компоненты UI:

- **Dashboard** - статистика и графики
- **Users Management** - таблица пользователей с CRUD
- **API Keys Management** - управление ключами
- **Audit Logs Viewer** - просмотр логов с фильтрами
- **Settings Panel** - конфигурация системы
- **OAuth Connections** - управление OAuth подключениями
- **Login Page** - вход для админов
- **Profile Settings** - настройки своего профиля (включая 2FA)

### Безопасность UI:

- Защита от XSS (санитизация HTML)
- CSRF токены (если используются cookies)
- Валидация на стороне клиента
- Маскирование sensitive данных (API ключи показывать только префиксы)
- Подтверждение критических операций (удаление, блокировка)
- Логаут при истечении токена

---

## Пример User Flow

1. **Админ входит** → `/auth/signin`
2. **Получает JWT токены** → сохраняет в memory/localStorage
3. **Загружает дашборд** → `GET /admin/stats`
4. **Просматривает пользователей** → `GET /admin/users?page=1&page_size=20`
5. **Блокирует пользователя** → `PUT /admin/users/:id` с `{"is_active": false}`
6. **Просматривает audit log** → `GET /admin/audit-logs?user_id=xxx`
7. **Отзывает API ключ** → `POST /admin/api-keys/:id/revoke`

---

## Макет страниц

### Dashboard
```
┌─────────────────────────────────────────┐
│  📊 Статистика                          │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│  │1,234 │ │  956 │ │  789 │ │  123 │   │
│  │Users │ │Active│ │2FA On│ │API Ks│   │
│  └──────┘ └──────┘ └──────┘ └──────┘   │
│                                         │
│  📈 Регистрации (последние 30 дней)    │
│  [График линейный]                      │
│                                         │
│  📋 Последние логины                    │
│  [Таблица с 10 последними входами]     │
└─────────────────────────────────────────┘
```

### Users Management
```
┌─────────────────────────────────────────┐
│  👥 Пользователи                        │
│  [Поиск] [Фильтр: Роль▼] [+Создать]   │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │Email      │Username│Role │2FA│···│  │
│  ├───────────────────────────────────┤  │
│  │user@ex.com│john   │admin│✓  │⚙️ │  │
│  │jane@ex.com│jane   │user │✗  │⚙️ │  │
│  └───────────────────────────────────┘  │
│                                         │
│  [← Prev] Page 1 of 10 [Next →]       │
└─────────────────────────────────────────┘
```

### API Keys Management
```
┌─────────────────────────────────────────┐
│  🔑 API Ключи                           │
│  [Поиск] [Фильтр: Статус▼] [Владелец]  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │Название  │Префикс  │Владелец│···│  │
│  ├───────────────────────────────────┤  │
│  │Prod Key  │agw_Ab12 │john   │⚙️ │  │
│  │Test Key  │agw_Cd34 │jane   │⚙️ │  │
│  └───────────────────────────────────┘  │
│                                         │
│  [← Prev] Page 1 of 5 [Next →]        │
└─────────────────────────────────────────┘
```

---

Этот документ содержит всю необходимую информацию для разработки полнофункционального UI административной панели для Auth Gateway.
